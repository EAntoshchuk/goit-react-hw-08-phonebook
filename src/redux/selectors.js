export const getContactSelector = state => state.contacts.contacts;
export const isLoadingSelector = state => state.contacts.loading;
export const errorSelector = state => state.comtacts.error;
export const filterSelector = state => state.filter.filter;
